Make sure that you first create the instance using this configuration.
That means running:
terraform init
terraform plan
terraform apply

You must have deployed the instance first, and then you can make the 
modifications as performed in this demo.